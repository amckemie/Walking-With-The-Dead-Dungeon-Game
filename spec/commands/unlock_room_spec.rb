require 'spec_helper'

describe 'unlock room' do
  it "returns success true if the player uses an item of type key that stores the room's name" do
  end

  it "changes a room's locked status to false" do
  end

  it "returns success false and an error message when the player uses the incorrect key" do
  end

  it "the room's locked status remains true" do
  end
end

